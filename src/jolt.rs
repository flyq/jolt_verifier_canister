// pub use ark_bn254::{Fr as F, G1Projective as G};
use jolt_core::jolt::vm::{
    bytecode::BytecodeRow,
    rv32i_vm::{RV32IJoltProof, RV32IJoltVM, RV32I},
    Jolt, JoltCommitments, JoltPreprocessing, JoltProof,
};

// pub fn deserialize_proof(proof: &[u8]) ->  RV32IJoltProof<F, G> {
//     RV32IJoltProof<F, G>::deserialize_compressed(proof).unwrap()

// }

// pub fn deserialize_commitments(commitments: &[u8]) -> JoltCommitments<G> {
//     RV32IJoltProof<F, G>::deserialize_compressed(commitments)

// }
